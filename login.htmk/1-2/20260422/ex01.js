function solution(num1, num2){
    return Math.floor(num1 / num2);
}

// Document 브라우저 프론트

// Math objet 자료형 node 기본 지원 되는 객체
// round 반올림
// floor 내림
// ceil 올림
// random() 0<x<1 사이 숫자 반환

const ret = solution(9, 5); // 1
console.log(ret)