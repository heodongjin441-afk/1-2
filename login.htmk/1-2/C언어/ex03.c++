#include <stdio.h>
int main(){
    // int a, b = 1;
    // scanf("%d %d", &a, &b); // 공백으로 구분하여 입력받음
    // printf("%d %d", a, b);
    float a = 1.998;
    // scanf("%f", &a); // 실수형 입력받음
    printf("%.2f", a); // 실수형은 printf("%0.2f", a);로 출력됨 
    return 0;
}